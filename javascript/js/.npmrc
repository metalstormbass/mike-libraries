# NPM Configuration for Registry 1
# Public npmjs.com (Default)

# Public npm registry - no authentication required
registry=https://registry.npmjs.org/

# General Settings
strict-ssl=true
loglevel=info

# Proxy settings (if needed)
# proxy=http://proxy.example.com:8080
# https-proxy=http://proxy.example.com:8080
